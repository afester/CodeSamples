/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package SampleApp.app;

import SampleApp.list.LinkedList;

import static SampleApp.utilities.StringUtils.join;
import static SampleApp.utilities.StringUtils.split;
import static SampleApp.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
